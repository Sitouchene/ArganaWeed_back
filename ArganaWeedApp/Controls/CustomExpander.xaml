<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ArganaWeedApp.Controls.CustomExpander">
    <StackLayout>
        <Grid x:Name="HeaderContainer" ColumnDefinitions="*, Auto">
            <Label x:Name="HeaderLabel" FontSize="20" VerticalOptions="Center" />
            <Label x:Name="ExpandButton" Text="˅" FontSize="20" VerticalOptions="Center" HorizontalOptions="End" />
        </Grid>
        <BoxView Color="Black" HeightRequest="1" HorizontalOptions="FillAndExpand" />
        <ContentView x:Name="ContentContainer" IsVisible="False">
            <ContentPresenter />
        </ContentView>
    </StackLayout>
</ContentView>
